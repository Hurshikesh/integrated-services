/** @type {import('next').NextConfig} */
const nextConfig = {
  
  images: {
    domains: ['lh3.googleusercontent.com', 'your-image-domain.com','i.postimg.cc','avatars.githubusercontent.com']
  },
};

export default nextConfig;
